<!DOCTYPE html>
<html lang="en">
<head>
  <meta name="viewport" content="initial-scale=1.0, user-scalable=no" />  
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />  
  <title><%= name %></title>
  <link href="/stylesheets/imageActivity.css" rel="stylesheet">
  <script type="text/javascript" src="/javascripts/common/sendMassage.js"></script>
</head>
<body>
  <div class="image_container">
    <img src="<%= bigPicture %>" alt="" class="activity_image">
    <div class="activity_detail_container">
      <button class="activity_detail_button">点击查看活动内容</button>
    </div>
  </div>
  <% if(!close) { %>
  <div class="join_container">
    <p class="activity_name">#本期活动#<%= name %></p>
    <button class="join_button" onclick="joinActivity()">立即参与</button>
  </div>
  <% } %>
  <script type="text/javascript">
    window.activity = {}
    activity.name = '<%= name %>'
    activity.tpid = '<%= tpid %>'
    activity.picture = '<%= picture %>'
    function activityDetail() {
      var activity = {
        imageUrl: activity.picture,
        topic: activity.name,
        tpid: activity.tpid
      }
      showActivityDetail(JSON.stringify(activity))
    }
  </script>
</body>
</html>
